name: undo_example
description: Demonstrates how to use the undo plugin.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  undo:
    path: ../

flutter:
  uses-material-design: true

